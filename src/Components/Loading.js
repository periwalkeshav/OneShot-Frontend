import React from 'react';
import '../App.css'
const Loading = () => {
    return (
        <div className="loading">
            <h1>Loading...</h1>
        </div>
    );
};

export default Loading;
